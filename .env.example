# MongoDB Connection
# For MongoDB Atlas (cloud), use the connection string from your cluster
# Replace <password> with your actual password and ensure special characters are URL-encoded
# Example: @ becomes %40, # becomes %23, etc.
MONGO_URI=mongodb+srv://shaunsebastian4_db_user:Gtasan%401998@cluster0.ym5fid3.mongodb.net/portfolio?retryWrites=true&w=majority&appName=Cluster0

# Or for local MongoDB:
# MONGO_URI=mongodb://localhost:27017/portfolio

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret (change this to a random string in production)
JWT_SECRET=supersecret
